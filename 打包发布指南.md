# 打包发布指南

## 项目结构

你的项目包含两个独立的 Electron 应用：

```
Screenshot tool/
├── electron/           # 用户端应用（截图工具）
│   ├── package.json
│   ├── main.js
│   └── ...
└── license-tool/      # 管理端应用（授权签发工具）
    ├── package.json
    ├── main.js
    └── ...
```

---

## 打包流程

### 准备工作（首次打包）

#### 1. 安装依赖

```bash
# 用户端应用
cd electron
npm install

# 授权工具
cd ../license-tool
npm install
```

#### 2. 检查配置

确认两个 `package.json` 的配置正确：
- ✅ `electron/package.json` - 已更新，包含所有文件
- ✅ `license-tool/package.json` - 配置正确

---

## 打包命令

### 方式一：分别打包（推荐）

#### 1. 打包用户端应用

```bash
cd electron
npm run package
```

**输出**：
- 目录：`electron/release/`
- 文件：`镜核 Setup 0.1.0.exe`（约 200-300MB）

#### 2. 打包授权工具

```bash
cd ../license-tool
npm run package
```

**输出**：
- 目录：`license-tool/release/`
- 文件：`镜核授权签发工具 Setup 1.0.0.exe`（约 150-250MB）

---

### 方式二：使用脚本批量打包

创建打包脚本 `build-all.bat`：

```batch
@echo off
echo ========================================
echo 开始打包镜核应用
echo ========================================

echo.
echo [1/2] 打包用户端应用...
cd electron
call npm install
call npm run package
if %ERRORLEVEL% NEQ 0 (
    echo 用户端应用打包失败！
    pause
    exit /b 1
)

echo.
echo [2/2] 打包授权工具...
cd ../license-tool
call npm install
call npm run package
if %ERRORLEVEL% NEQ 0 (
    echo 授权工具打包失败！
    pause
    exit /b 1
)

echo.
echo ========================================
echo 打包完成！
echo ========================================
echo 用户端应用: electron\release\镜核 Setup 0.1.0.exe
echo 授权工具: license-tool\release\镜核授权签发工具 Setup 1.0.0.exe
echo ========================================
pause
```

使用方法：
```bash
# 在项目根目录运行
.\build-all.bat
```

---

## 打包测试（推荐）

在正式打包前，建议先测试：

### 1. 开发模式测试

```bash
# 用户端
cd electron
npm run dev

# 授权工具
cd license-tool
npm start
```

### 2. 打包测试（不生成安装程序）

```bash
# 用户端
cd electron
npm run package:dir

# 授权工具
cd license-tool
npm run package:dir
```

这会在 `release/win-unpacked/` 生成未打包的可执行文件，可以快速测试。

---

## 打包注意事项

### 用户端应用（electron）

**依赖检查**：
- ✅ `ddddocr` - OCR 识别库
- ✅ `pngjs` - PNG 处理
- ✅ `xlsx` - Excel 生成
- ⚠️ 确保所有 `.js` 文件都被包含（已在 package.json 中配置 `*.js`）

**关键文件**：
```json
"files": [
  "*.js",           // 包含所有 JS 文件
  "!*.test.js",     // 排除测试文件
  "../app/**/*",    // 前端界面
  "package.json"
]
```

### 授权工具（license-tool）

**关键文件**：
- `signer.js` - 签名逻辑（包含私钥，**不要泄露**）
- `main.js` - 主进程
- `index.html` - 界面

**安全提示**：
- ⚠️ 授权工具仅供内部使用，不要分发给最终用户
- ⚠️ 私钥文件必须妥善保管

---

## 版本管理

### 更新版本号

**用户端**：
```bash
cd electron
# 编辑 package.json，修改 version 字段
# "version": "0.1.0" -> "0.2.0"
```

**授权工具**：
```bash
cd license-tool
# 编辑 package.json，修改 version 字段
# "version": "1.0.0" -> "1.0.1"
```

### 版本号规则

采用语义化版本：`主版本.次版本.修订号`

- **主版本**：重大功能变更或不兼容更新
- **次版本**：新增功能，向下兼容
- **修订号**：Bug 修复，小改进

示例：
- `0.1.0` -> `0.1.1` - Bug 修复
- `0.1.1` -> `0.2.0` - 新增功能
- `0.2.0` -> `1.0.0` - 正式发布

---

## 分发清单

### 给最终用户

**必须提供**：
1. `镜核 Setup x.x.x.exe` - 用户端安装程序
2. `用户指南.md` - 使用说明
3. `.jinghe-license` 授权文件（根据机器码生成）

**可选提供**：
- 快速开始指南
- 视频教程
- 常见问题文档

### 内部使用

**授权管理**：
1. `镜核授权签发工具 Setup x.x.x.exe` - 授权工具安装程序
2. `signer.js` 备份（包含私钥，务必安全保存）
3. 授权签发流程文档

---

## 打包后检查清单

### ✅ 用户端应用检查

在干净的 Windows 系统上测试：
- [ ] 安装程序可以正常安装
- [ ] 应用可以正常启动
- [ ] 系统检查功能正常（浏览器检测、权限检查）
- [ ] 无授权时提示正确
- [ ] 导入授权后可以正常使用
- [ ] 查询功能正常
- [ ] 生成的截图和报告正确
- [ ] 卸载功能正常

### ✅ 授权工具检查

- [ ] 安装程序可以正常安装
- [ ] 可以读取机器码
- [ ] 可以生成授权文件
- [ ] 生成的授权文件格式正确
- [ ] 授权文件签名验证通过

---

## 常见打包问题

### 1. 打包失败：找不到模块

**原因**：依赖未正确安装

**解决**：
```bash
cd electron  # 或 license-tool
rm -rf node_modules
rm package-lock.json
npm install
npm run package
```

### 2. 打包后应用无法启动

**可能原因**：
- 文件未正确打包
- 路径问题
- 依赖缺失

**检查方法**：
```bash
# 使用 package:dir 检查文件
npm run package:dir
# 查看 release/win-unpacked/ 目录内容
```

### 3. 打包体积过大

**正常体积**：
- 用户端：200-300MB
- 授权工具：150-250MB

**如果超过 500MB**：
- 检查是否包含了不必要的文件
- 检查 node_modules 是否有冗余

### 4. 打包很慢

**原因**：第一次打包需要下载 Electron 二进制文件

**优化**：
- 使用国内镜像：
  ```bash
  set ELECTRON_MIRROR=https://npmmirror.com/mirrors/electron/
  npm install
  ```

---

## 发布流程建议

### 1. 开发阶段
- 频繁测试，使用 `npm run dev` 或 `npm start`
- 定期提交代码到 Git

### 2. 测试阶段
- 使用 `npm run package:dir` 快速打包测试
- 在多个 Windows 版本上测试

### 3. 发布阶段
- 更新版本号
- 更新用户文档
- 执行完整打包：`npm run package`
- 在干净系统上完整测试
- 准备发布文件和文档

### 4. 发布后
- 收集用户反馈
- 记录已知问题
- 规划下一版本

---

## 快速参考

### 开发命令

```bash
# 用户端 - 开发模式
cd electron && npm run dev

# 授权工具 - 开发模式
cd license-tool && npm start
```

### 打包命令

```bash
# 用户端 - 完整打包
cd electron && npm run package

# 授权工具 - 完整打包
cd license-tool && npm run package

# 快速测试打包
npm run package:dir
```

### 清理命令

```bash
# 清理打包输出
rm -rf electron/release
rm -rf license-tool/release

# 清理依赖（需要重新 npm install）
rm -rf electron/node_modules
rm -rf license-tool/node_modules
```

---

## 自动化构建（进阶）

如果需要自动化构建流程，可以考虑：

1. **GitHub Actions** - 自动化打包和发布
2. **Jenkins** - 内部持续集成
3. **批处理脚本** - 本地自动化（见上面的 build-all.bat）

---

**文档版本**: 1.0
**更新日期**: 2026-08-24
