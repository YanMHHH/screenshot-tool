# 企业公开信息查询与留存工具需求文档

## 1. 文档信息

| 项目 | 内容 |
| --- | --- |
| 产品名称 | 企业信用截图工具（镜核） |
| 文档版本 | 2026-08-20 反推版 |
| 文档状态 | 与当前代码实现一致 |
| 运行形态 | Windows 本地 Electron 桌面应用 |
| 文档目的 | 说明当前已实现的业务范围、功能规则和限制，为验收及后续迭代提供依据 |

本文件以当前代码为准。历史规划中存在但当前版本尚未实现的能力，不视为本版本需求。

## 2. 背景与目标

招投标审查、供应商准入等场景需要针对一批企业，在公开网站查询失信相关信息并留存可追溯证据。人工操作需要重复输入企业名称、处理图形验证码、截图、命名、汇总，且在页面异常或中断后难以恢复。

本版本目标是将“企业名称 x 已适配查询项”转为串行本地任务，自动查询、识别图形验证码、保留查询结果、记录执行明细并归档。工具只保存网站查询页面及执行结果，不对企业是否合格作业务判断。

## 3. 适用范围与边界

### 3.1 已支持网站

| 编号 | 网站 | 查询项 | 查询入口 | 状态 |
| --- | --- | --- | --- | --- |
| W-001 | 信用中国 | 严重失信主体名单 | `https://www.creditchina.gov.cn/xinxigongshi/shixinheimingdan/` | 已适配 |
| W-001 | 信用中国 | 重大税收违法失信主体名单 | `https://www.creditchina.gov.cn/zhuanxiangchaxun/zhongdashuishouweifaanjian/` | 已适配 |
| W-002 | 中国执行信息公开网 | 失信被执行人 | `https://zxgk.court.gov.cn/shixin/` | 已适配 |
| W-003 | 中国政府采购网 | 政府采购严重违法失信行为记录名单 | `https://www.ccgp.gov.cn/search/cr/` | 已适配 |
| W-004 | 军队采购网 | 军队采购暂停名单 | `https://www.plap.mil.cn/freecms-glht/site/juncai/jdjc/index.html?channel=ad1a7596-65eb-48b4-909f-11679731ae94` | 已适配 |
| W-004 | 军队采购网 | 军队采购失信名单 | `https://www.plap.mil.cn/freecms-glht/site/juncai/jdjc/index.html?channel=ad1a7596-65eb-48b4-909f-11679731ae94` | 已适配 |
| W-005 | 全国企业破产重整案件信息网 | 企业破产重整案件信息 | `https://pccz.court.gov.cn/pcajxxw/index/xxwsy` | 已适配 |

W-006 仅在界面中展示为待适配项，不能选择，也不会执行。

### 3.2 不在当前范围

- 登录、账号管理、云端同步、协同、任务历史列表和在线分享。
- 对查询结果作业务结论、风险评级或合格判定。
- 手工图片编辑、标注、录屏、通用桌面截图和 OCR 文字提取。
- 其他未列入“已支持网站”的站点自动化。
- 对验证码成功率作保证。验证码识别或服务端校验失败会被如实记录。
- 真实 License 校验。设置页仅展示“开发版（未激活）”说明。

## 4. 用户与运行环境

### 4.1 目标用户

- 招投标审查人员：创建项目、输入企业名单、选择网站并执行留存。
- 复核或归档人员：查看 CSV 执行明细、证据文件和 ZIP 包。

### 4.2 环境要求

- Windows 10 或 Windows 11。
- 可访问目标公开网站的网络。
- 本机已安装 Google Chrome 或 Microsoft Edge。程序优先使用 Chrome，未找到 Chrome 时使用 Edge。
- PNG 留存时，主显示器底部 Windows 任务栏应处于可见状态；任务栏自动隐藏会导致该项截图保存失败。
- 用户选择的输出目录可写，且有足够磁盘空间。

### 4.3 数据边界

- 企业名单、任务状态、截图、PDF、CSV 和 ZIP 均写入本机，应用不提供额外的云端上传、同步或分享服务。
- 为完成查询，企业名称和验证码提交会直接发送至用户所选的目标公开网站。
- 查询使用项目独立的浏览器配置目录，不复用用户日常浏览器的配置或标签页。
- 程序会回收本项目遗留的专用浏览器进程，不会主动关闭用户正常使用的 Chrome 或 Edge 会话。

## 5. 主流程

```text
填写项目名称和企业名单
  -> 选择一个或多个已适配网站
  -> 选择 PNG 或网页 PDF 留存方式
  -> 选择输出目录并开始任务
  -> 逐企业、逐查询项执行
  -> 输入企业名称、识别图形验证码、提交查询
  -> 判断成功、无记录或失败
  -> 保存证据、CSV 明细和任务状态
  -> 生成 ZIP 归档包并展示汇总结果
```

执行顺序为串行：先处理第一家企业的所有已选查询项，再处理下一家企业。若开启随机延迟，相邻完成项之间会等待 1 至 3 秒。

## 6. 功能需求

### 6.1 任务配置

| 功能 | 规则 |
| --- | --- |
| 项目名称 | 必填，最长 80 个字符；用于输出目录。Windows 文件名不支持的字符会替换为下划线。 |
| 企业名单 | 必填，支持手动逐行输入或用逗号分隔；会去除首尾空格、过滤空项和完全重复项。 |
| 导入名单 | 支持 `.txt`、`.csv`、`.xls`、`.xlsx`，读取首列。单批最多 500 家。 |
| 网站选择 | 至少选择 W-001 或 W-002 之一，可组合执行；选择会作为任务状态的一部分保存。 |
| 留存方式 | 二选一：PNG 截图或网页 PDF；同一任务内统一使用一种方式。 |
| 输出路径 | 必填，可使用目录选择器；默认值为桌面路径并保存在本机浏览器存储中。 |
| 设置 | 可设置站点重试次数、验证码重试次数，范围均为 1 至 10；可开关 1 至 3 秒随机延迟。 |

### 6.2 W-001 信用中国查询

1. 打开对应查询页。
2. 填写企业名称并提交。
3. 若出现图形验证码，等待图片稳定后截取验证码区域。
4. 使用本地 `ddddocr` 进行多版本识别并提交结果。
5. 验证码失败时，刷新验证码或重新发起查询，直至达到验证码重试上限。
6. 根据结果页面判断“成功”或“无记录”，保存结果页；页面或网络异常按站点重试次数处理。

### 6.3 W-002 中国执行信息公开网查询

1. 打开失信被执行人查询页。
2. 在“被执行人姓名/名称”输入企业名称。
3. 读取页面图形验证码，使用本地 `ddddocr` 识别并填写验证码。
4. 点击查询，等待结果区域出现。
5. 页面提示“没有找到”或“相关的结果”时标记为“无记录”；出现结果行或企业名称时标记为“成功”。
6. 页面提示验证码错误或过期时，点击验证码图片刷新后重试；查询异常按站点重试次数重新进入整个流程。

### 6.4 执行控制与断点恢复

| 功能 | 规则 |
| --- | --- |
| 实时进度 | 显示当前企业、网站、查询项、状态、完成数和总数。 |
| 日志 | 显示浏览器启动、验证码识别、重试、证据保存和异常信息。 |
| 暂停 | 在当前流程的可中断检查点暂停；已写入的状态和文件保留。 |
| 继续 | 恢复后从未完成的“企业 x 网站 x 查询项”继续，不重复已完成项。 |
| 取消 | 停止后续执行，已完成文件和状态保留，并生成当前结果的报告与归档。 |
| 断点恢复 | 同一输出目录和项目名称下发现未完成 `task_state.json` 时，提示“从断点继续”或“重新开始”。 |
| 重新开始 | 当前实现会删除并重建同名项目目录后重新执行；如需保留旧证据，应使用不同项目名称或输出路径。 |

### 6.5 W-003 中国政府采购网查询

1. 打开“政府采购严重违法失信行为记录名单”入口页，并加载其同域查询 iframe。
2. 在“企业名称”输入企业名称并提交“查找”查询。
3. 使用查询后出现的“查询结果/查询内容”标记，或结果行数相对于首页默认列表发生变化，确认查询已完成，避免将首页默认列表误判为结果。
4. 页面提示“没有该企业的相关记录”时标记为“无记录”；查询完成且存在结果行时标记为“成功”。
5. 为保证截图和 PDF 包含完整查询内容，查询成功后会将 iframe 高度扩展至结果实际高度再留存。

### 6.6 W-004 军队采购网查询

1. 打开监督管理入口页，加载“军队采购暂停名单”和“军队采购失信名单”两个子栏目。
2. 每个子栏目均先切换名单并等待其默认列表完成加载，再在“关键字”中填写企业名称并点击查询，避免名单切换时的默认请求覆盖企业查询。
3. 结果显示“没有查询到相关记录”或“暂无数据”时标记为“无记录”；结果列表中存在企业名称时标记为“成功”。
4. 军队采购网的单次页面请求可能较慢；默认名单加载和企业查询各最多等待约 70 秒，超过后按站点重试次数重新尝试。

### 6.7 W-005 全国企业破产重整案件信息网查询

1. 打开网站首页，在首页搜索框输入企业名称并提交搜索。
2. 网站原始表单默认在新标签页显示结果；程序将其改为在专用浏览器的当前页面跳转，以便持续等待、判断和保存该结果页。
3. 页面显示“很抱歉，没有找到……相关结果”时标记为“无记录”；显示“共搜索出…条记录”且有结果行时标记为“成功”。
4. 成功或无记录后，按所选留存方式保存完整结果页；页面或网络异常按站点重试次数重新进入整个流程。

### 6.8 异常状态与执行状态

| 异常状态 | 判定规则 |
| --- | --- |
| 异常 | 页面检测到企业相关查询结果，且证据文件保存成功。 |
| 无异常 | 页面明确显示未找到相关结果，且证据文件保存成功。 |
| 查询异常 | 验证码未通过、页面结构变化、浏览器、网络、等待结果或文件保存失败，无法得出异常结论。 |

内部执行状态仍使用“成功”“无记录”“验证码未通过”“页面结构异常”“失败”“跳过”，用于任务重试、断点恢复、日志和汇总；任务结果表及 CSV 对外展示“异常状态”。

## 7. 证据留存与输出

### 7.1 PNG 截图

- 使用浏览器调试协议获取完整网页图片，页面宽度限制为 800 至 2400 像素。
- 截图页面高度上限为 20,000 像素；超过时保存上限部分并在执行明细中记录截断原因。
- 截图底部追加主显示器的完整 Windows 任务栏。因此图片中保留 Windows 日期和系统时间，且时间位于其原生右下角位置。
- 系统任务栏由当前桌面截取。它可能包含用户当前任务栏图标、通知区域或其他本机可见信息。

### 7.2 网页 PDF

- 使用 Chromium 原生打印接口生成 A4 横向 PDF。
- 保留页面背景。
- 每页左上角打印生成时的本机系统时间。
- 每页右下角打印“当前页/总页数”。

### 7.3 输出结构

任务目录为：

```text
<保存路径>/<项目名称>/
  task_state.json
  执行明细报告.csv
  <企业名称>/
    <网站名称>_<查询项>_<时间戳>.png 或 .pdf
  captcha-debug/
    <验证码诊断文件>
```

任务完成或取消后，会在项目目录的同级位置生成：

```text
<项目名称>_<时间戳>.zip
```

ZIP 包会排除 `task_state.json` 及临时状态文件，其他已生成内容会被归档。CSV 使用 UTF-8 BOM，字段为：序号、公司名称、网站、查询项、异常状态、留存方式、失败原因、留存文件名、时间。

### 7.4 验证码诊断

当验证码未稳定加载、识别无结果或校验失败时，程序会在 `captcha-debug` 中写入诊断 JSON；可用时也会保存验证码图片或验证码弹窗截图。该信息用于排查站点页面变化与识别失败。

## 8. 界面需求

| 页面 | 主要内容 | 主要操作 |
| --- | --- | --- |
| 任务配置 | 项目、企业名单、网站、留存方式和输出路径 | 导入、清空、选择网站、开始、断点恢复、重新开始 |
| 执行进度 | 进度条、当前状态、实时日志 | 暂停、继续、取消 |
| 任务结果 | 汇总信息和逐项执行明细 | 打开输出目录、打开 CSV 明细 |
| 设置 | 默认路径和重试策略 | 选择目录、保存设置 |

界面为简体中文桌面单窗口。任务在后台的专用浏览器窗口中执行，窗口会移至屏幕外，不应抢占鼠标、键盘或前台焦点；PNG 留存中的任务栏截取是例外，它会读取当前主显示器底部可见区域。

## 9. 异常处理与约束

| 场景 | 当前行为 |
| --- | --- |
| 未安装浏览器 | 提示未找到 Chrome 或 Edge，任务无法启动。 |
| 浏览器安全检查超时 | 最多等待 90 秒，失败后记录任务异常。 |
| 结果等待超时 | 单次查询结果等待最长约 30 秒，再按站点重试规则处理。 |
| 验证码图片加载异常 | 等待稳定加载；失败时刷新或重新查询并保存诊断。 |
| 页面结构变化 | 标记页面结构异常或失败，保存可用诊断信息。 |
| 输出目录不可写或保存失败 | 当前项记录失败原因，批次继续处理其他项。 |
| 网站响应或验证码策略变化 | 可能导致识别失败、结果超时或页面结构异常，需要更新适配器。 |

## 10. 非功能需求与验收标准

| 类别 | 验收标准 |
| --- | --- |
| 本地性 | 企业名单和留存文件不经应用上传；所有输出位于用户选择的本机目录。 |
| 可追溯性 | 每个已完成项都有一行 CSV 明细；成功或无记录项应有对应 PNG 或 PDF。 |
| 可恢复性 | 中断后可读取 `task_state.json`，继续时不重复已完成项。 |
| 浏览器隔离 | 专用浏览器使用独立 profile，并在任务结束时回收进程树。 |
| PNG 时效性 | 成功生成的 PNG 底部包含完整主显示器任务栏，从而包含系统时间和日期。 |
| PDF 时效性 | 成功生成的 PDF 每页左上角包含生成时的本机时间。 |
| 站点适配 | W-001 两个查询项、W-002 一个查询项、W-003 一个查询项、W-004 两个查询项和 W-005 一个查询项可被选择、启动和写入结果明细。 |
| 兼容限制 | 页面结构、验证码规则、网络、安全检查和任务栏可见状态均会影响实际成功率。 |

## 11. 后续迭代建议

1. 为每个网站增加受控的人工验证码接管，而非仅依赖 OCR 重试。
2. 将 CSV 升级为结构化 Excel 报告，并增加项目级汇总字段。
3. 为任务重新开始增加“保留旧目录”选项，降低同名项目覆盖风险。
4. 增加任务历史、失败项单独重试、站点适配版本和可视化浏览器预览。
5. 为已适配站点增加结果页结构变更监控与可控的失败项单独重试。
