# 镜核企业信用截图工具功能特性

## 已实现能力

### 多站点批量查询

- 支持一次输入最多 500 家企业名称。
- 支持手动输入、TXT、CSV、XLS 和 XLSX 导入，读取首列企业名称。
- 自动过滤空项并去重。
- 可单独或组合选择以下网站：
  - 信用中国：严重失信主体名单。
  - 信用中国：重大税收违法失信主体名单。
  - 中国执行信息公开网：失信被执行人。
  - 中国政府采购网：政府采购严重违法失信行为记录名单。
  - 军队采购网：军队采购暂停名单、军队采购失信名单。
  - 全国企业破产重整案件信息网：企业破产重整案件信息。
- 按“企业 x 查询项”串行执行，实时显示进度和运行日志。

### 图形验证码处理

- 使用本地 `ddddocr` 识别图形验证码，不把验证码图片上传至应用服务端。
- 对验证码图片进行稳定加载判断和多版本识别投票。
- 可设置 1 至 10 次验证码重试。
- 信用中国支持验证码刷新或重新发起查询。
- 中国执行信息公开网支持点击验证码图片刷新后重试。
- 验证码未通过、图片加载异常和识别失败会写入 `captcha-debug` 诊断信息。

### 两种证据留存方式

| 方式 | 文件 | 时效性信息 |
| --- | --- | --- |
| PNG 截图 | 完整网页 PNG | 图片底部附加完整 Windows 主任务栏，保留系统日期和时间。 |
| 网页 PDF | A4 横向 PDF | 每页左上角打印生成时的本机时间，右下角显示页码。 |

- 成功和无记录结果都会保存证据文件。
- PNG 采用浏览器全页面截图，最大高度为 20,000 像素；超出上限会记录截断原因。
- 证据文件按“企业目录/网站名称_查询项_时间戳”命名。

### 任务控制与恢复

- 运行中可暂停、继续或取消任务。
- 已完成项会实时写入任务状态文件和 CSV。
- 应用重开或任务中断后，可从断点继续，不重复已完成项。
- 可根据项目名和输出路径识别未完成任务。
- 可设置站点重试次数和 1 至 3 秒随机请求间隔。

### 本地结果与归档

- 每个项目生成 `执行明细报告.csv`，记录企业、网站、查询项、异常状态、留存方式、失败原因、文件路径和时间；查有记录显示“异常”，无记录显示“无异常”，无法完成查询显示“查询异常”。
- 任务结束或取消后自动生成 ZIP 归档包。
- 界面可直接打开输出目录和 CSV 明细。
- 所有企业名单、截图、PDF、明细和诊断默认只保存在本机；企业名称和验证码仅在查询时直接提交至所选公开网站。

### 浏览器隔离

- 使用独立的 Chrome 或 Edge 专用浏览器 profile，不使用用户日常浏览器配置。
- 优先使用 Chrome，未安装 Chrome 时回退到 Edge。
- 专用浏览器窗口移至屏幕外执行，不应影响用户前台操作。
- 任务前会清理同一专用 profile 的遗留浏览器进程，结束后回收本次进程树。

## 当前限制

- 目标网站验证码、安全检查、网络状态或页面结构变化可能导致某项失败，无法保证验证码必然通过。
- PNG 需要 Windows 主显示器底部任务栏可见；自动隐藏任务栏时无法保留完整系统时间。
- PDF 和 PNG 均保存查询结果页面，不提供人工图片编辑或批注。
- 报告格式目前为 CSV，不是 Excel 工作簿。
- 同一输出路径下使用“重新开始”会重建同名项目目录；需要保留旧结果时应改用新的项目名称或输出路径。
